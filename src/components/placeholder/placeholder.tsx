const Placeholder = () => {
  return (
    <div>
      Welcome! Check user profiles by adding them in address bar! Format
      "http://localhost:5173/profile?username=johndoe"
    </div>
  );
};

export default Placeholder;
